# ai-core Environment Variables
# ================================
# ai-core supports dual-model routing: heavy (expensive) for complex tasks,
# light (cheap) for simple tasks. This saves costs while maintaining quality.

# ===========================================
# LIGHT PROVIDER (default for simple tasks)
# ===========================================
# Tasks: SEO, frontend simple, tests, styles, meta tags
# Recommended: MiniMax (cheap), or any model

LLM_PROVIDER_LIGHT=minimax
LLM_API_KEY_LIGHT=tu-minimax-api-key
LLM_MODEL_LIGHT=MiniMax-Text-01
LLM_BASE_URL_LIGHT=
LLM_TEMPERATURE_LIGHT=0.7
LLM_MAX_TOKENS_LIGHT=2048

# ===========================================
# HEAVY PROVIDER (for complex tasks)
# ===========================================
# Tasks: architecture, security, database schema, integrations
# Recommended: Claude Sonnet 4, GPT-4, or similar
# Optional: If not set, will use light provider for everything

# LLM_PROVIDER_HEAVY=anthropic
# LLM_API_KEY_HEAVY=tu-anthropic-api-key
# LLM_MODEL_HEAVY=claude-sonnet-4-20250514
# LLM_BASE_URL_HEAVY=
# LLM_TEMPERATURE_HEAVY=0.7
# LLM_MAX_TOKENS_HEAVY=4096

# ===========================================
# LEGACY (single provider mode)
# ===========================================
# If you only have one API key, use these instead:
# LLM_PROVIDER=minimax
# LLM_API_KEY=tu-api-key
# LLM_MODEL=MiniMax-Text-01

# ===========================================
# Kilo API (optional)
# ===========================================
# KILO_API_URL=http://localhost:3000/api
# KILO_API_KEY=tu-kilo-api-key

# ===========================================
# Memory (optional)
# ===========================================
# AI_CORE_TTL_DAYS=30
# AI_CORE_MEMORY_DIR=~/.ai-core/projects

# ===========================================
# Deterministic mode (no LLM)
# ===========================================
# Leave all LLM variables commented to use deterministic mode
# The system will use keyword matching and predefined proposals
